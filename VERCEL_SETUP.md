# 🚀 Инструкция по деплою на Vercel с KV

## Шаг 1: Подготовка проекта

Все уже готово! API переписаны для работы с Vercel KV.

## Шаг 2: Создание проекта на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New"** → **"Project"**
3. Импортируйте ваш Git репозиторий (GitHub/GitLab/Bitbucket)
4. Vercel автоматически определит SvelteKit
5. Нажмите **"Deploy"**

## Шаг 3: Настройка Vercel KV (ВАЖНО!)

### 3.1 Создание KV базы данных

1. Откройте ваш проект на Vercel
2. Перейдите в **"Storage"** (в верхнем меню)
3. Нажмите **"Create Database"**
4. Выберите **"KV"** (Redis)
5. Выберите регион (ближайший к вашим пользователям)
6. Нажмите **"Create"**

### 3.2 Подключение KV к проекту

1. После создания базы данных, нажмите **"Connect Project"**
2. Выберите ваш проект из списка
3. Нажмите **"Connect"**

**Готово!** Vercel автоматически добавит переменные окружения:
- `KV_REST_API_URL`
- `KV_REST_API_TOKEN`
- `KV_REST_API_READ_ONLY_TOKEN`
- `KV_URL`

## Шаг 4: Redeploy

После подключения KV базы данных:

1. Перейдите в **"Deployments"**
2. Найдите последний деплой
3. Нажмите **"..."** → **"Redeploy"**
4. Подтвердите

## Шаг 5: Проверка

Откройте ваш сайт и проверьте:
- ✅ Счетчик посетителей работает
- ✅ Можно добавлять комментарии
- ✅ Комментарии сохраняются

## 📊 Бесплатный план Vercel KV

- **256 MB** хранилища
- **3000** команд в день
- **Навсегда бесплатно!**

Этого более чем достаточно для вашего сайта!

## 🔧 Локальная разработка (опционально)

Если хотите тестировать локально:

1. Установите Vercel CLI:
```bash
pnpm add -g vercel
```

2. Войдите в аккаунт:
```bash
vercel login
```

3. Подключите проект:
```bash
vercel link
```

4. Скачайте переменные окружения:
```bash
vercel env pull
```

5. Запустите dev сервер:
```bash
pnpm dev
```

## ❓ Проблемы?

### Ошибка "kv is not defined"
- Убедитесь, что KV база данных создана и подключена к проекту
- Сделайте redeploy после подключения

### Данные не сохраняются
- Проверьте, что переменные окружения установлены (Settings → Environment Variables)
- Убедитесь, что сделали redeploy после настройки KV

### Счетчик показывает 0
- Это нормально для новой базы данных
- Обновите страницу несколько раз - счетчик начнет расти

## 🎉 Готово!

Ваш сайт теперь полностью работает на Vercel с бесплатным хранилищем данных!
